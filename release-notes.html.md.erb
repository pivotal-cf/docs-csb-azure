---
title: Release Notes for Cloud Service Broker for Azure
owner: Cloud Service Broker
---

<strong><%= modified_date %></strong>

These are release notes for <%= vars.product_full %>.

##<a id="1-3-0"></a> v1.3.0

**Release Date:** MMM DD, YYYY


### Features

New features and changes in this release:

* **RUN-IN HEADING:** DESCRIPTION


### Resolved Issues

This release has the following fixes:

* **For `csb-azure-mssql-failover-group` Serverless databases can be created:** Databases can now be created in serviceless tier by defining a `sku_name` from `GP_S` tier.

* **Revised `mini` plan examples provided in the tile:** For new CSB tile installations all `mini` plans are now correct.
For existing tile installations that still have the following plans: `csb-azure-redis/mini`, `csb-azure-mysql/mini`, `csb-azure-mssql-failover-group/mini-manual-v2` you need to make the following changes to the existing plans.
If you want to use `csb-azure-redis/mini` add the following properties `tls_min_version` and `firewall_rules`.
If you want to use `csb-azure-mysql/mini` add the following properties `tls_min_version`, `firewall_rules`, `authorized_networks`.
If you want to use `csb-azure-mssql-failover-group/mini-manual-v2` replace `storage_gb` property name with `max_storage_gb`.

* **RUN-IN HEADING:** DESCRIPTION


### Known Issues

This release has the following issues:

* **RUN-IN HEADING:** DESCRIPTION


## <a id="view"></a> View Release Notes for Another Version

To view the release notes for another product version, select the version from
dropdown at the top of this page.
